export default function HomePage() {
  return (
    <main>
      <section className="hero">
        <p className="eyebrow">Token-bound NFT primitive</p>
        <h1>NeuralMint</h1>
        <p className="lede">
          Generate an NFT, lock 1,000,000 $NMINT behind a deterministic vault, and trade the
          unwrap rights on any marketplace. The program is the only signer.
        </p>
      </section>
    </main>
  );
}
